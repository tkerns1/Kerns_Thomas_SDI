/**
 * Thomas Kerns
 * SDI
 * 201503-02
 * External Worksheets
 */

var item = "shirt";
var originalPrice = 10;
var discount    =   20;
var salesTax = 8;
var discountedPrice = originalPrice * discount%;
var withTax = (originalPrice * discount%) * salesTax%;

console.log("Your " + item + " was originally $" + originalPrice + "after the discount $" + discountedPrice + " and ") ;
//" before taxes, after taxes" + withTax + "is your total");
