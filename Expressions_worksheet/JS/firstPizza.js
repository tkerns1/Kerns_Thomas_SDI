/**
 * Thomas Kerns
 * SDI
 * 201503-02
 * External Worksheets
 */

var partyPeople = 10;
var orderedPizzas = 3;
var slicesPizza = orderedPizzas * 8;
var slicesPer = slicesPizza/10;

console.log("Each person ate " + slicesPer + " slices of pizza at the party")
console.log("there were " + partyPeople + " at the party" )